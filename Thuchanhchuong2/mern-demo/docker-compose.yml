services:
  backend:
    build: ./server
    container_name: mern-backend
    ports:
      - "5000:5000"
    environment:
      - MONGODB_URI=${MONGODB_URI}

  frontend:
    build: ./client
    container_name: mern-frontend
    ports:
      - "3000:5173"
    depends_on:
      - backend